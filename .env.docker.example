# Copy to .env.docker (gitignored) to provide server-only secrets to the
# Docker dev container. Not needed for the UI routes covered by this audit,
# only for server functions that use the Supabase service role
# (src/integrations/supabase/client.server.ts).
SUPABASE_SERVICE_ROLE_KEY=
